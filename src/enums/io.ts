export enum Direction {
  forward = 1,
  backward = -1,
}

export enum KeyActions {
  keydown = "keydown",
  keyup = "keyup",
}

export enum KeyCodes {
  arrowRight = "ArrowRight",
  arrowLeft = "ArrowLeft",
  space = "Space",
  keyD = "KeyD",
}
